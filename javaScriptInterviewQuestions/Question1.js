//What would be the output

const obj = {
    a:'one',
    b: 'two',
    a: 'three',

    //if you have two keys with the same name the first key will be replaced

    //it will be in the same position with the last specified value
};

console.log(obj)